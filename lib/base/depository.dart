part of "_.dart";

abstract class base__depository__base /*
`::dispose` should be called ,only once
derived `::dispose`s should begin with calling `super::dispose` */ //
    implements
        base__dispose__protocol {
  const base__depository__base();
}
